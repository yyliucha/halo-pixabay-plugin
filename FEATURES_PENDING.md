# 公告 / 决策记录：应用市场上架暂缓（2026-08-28）

## 事件经过

1. **v1.1.0** 提交 Halo 应用市场首次审核，被驳回，意见：
   - 与现有 Image Stream 插件（附件选择时搜索并转存 Pixabay 等图源）功能重叠，缺乏差异化场景
   - 自有代码使用 `run.halo.*` 保留命名空间（要求迁移）
   - AI 深度参与声明 → 提交前需充分人工测试
2. **v1.2.0** 完成整改（已提交 GitHub，Release 已建）：
   - 命名空间迁移 `run.halo.pixabay` → `com.yyliucha.pixabay`
   - 新增差异化功能：发布文章自动配图、关键词→策略/分组映射归档
   - 单元测试扩至 18 项全通过；修复新建附件分组 NPE
3. **复核发现**：官方 plugin-image-stream 虽无自动配图/定时/映射能力（源码实证），但**核心类别相同**（图片平台→Halo 附件库），且对方内置 API Key、零门槛、生态背书；同赛道内我们处劣势。开发者决策：**暂缓上架**。

## 决策（开发者，2026-08-28）

- 停止商店上架流程；插件继续**自用 + GitHub Release 分发**（https://github.com/yyliucha/halo-pixabay-plugin/releases）
- v1.2.0 代码保留并正常使用（发布文章自动配图对自己的博客直接可用）
- 若未来重启上架，候选方向（见下方 A 方案）：

## 未来重启上架的候选方向（A：文章配图工作流）

以**文章**为第一公民，与官方"编辑器内手动搜索转存"彻底区隔：

- 发布文章自动配图（已有）
- 一键为历史无封面文章批量补图（未实现，主差异化点）
- 按文章分类/标签自动扩充对应图库（与关键词映射结合）
- 改名强化定位，如「Pixabay 配图助手」
- 对审核说辞："自动 vs 手动、批量 vs 单篇、发布时 vs 编辑时"

---

## 附：待实现功能：关键词 ↔ 存储策略/附件分组一一对应

> 状态：**已实现于 v1.2.0**（`keywordMappings` 设置项 + 分组下载逻辑，见
> `PixabaySetting.KeywordMapping`、`PixabayDownloadService.groupKeywords/runGroup`）

## 需求背景

当前插件所有搜索关键词共用一套「附件存储策略 + 附件分组」配置，下载的图片全部落入
同一个策略/分组。用户希望**每个搜索关键词可以独立指定存储策略和附件分组**，按图片
类型/主题分门别类地入库。

## 典型场景

| 关键词 | 策略 | 分组 |
|---|---|---|
| mountain, landscape（风景类） | 本地存储 | 风景图片 |
| flower, plant（植物类） | 对象存储 OSS/S3 | 植物图片 |
| ... | ... | ... |

## 实现要点（已落地）

1. 设置页 `$formkit: repeater` 渲染「关键词—策略/分组」条目（复用内置
   `attachmentPolicySelect` / `attachmentGroupSelect` 下拉）
2. `PixabaySetting.keywordMappings`（keyword/policy/group 三元组），旧全局字段保留兜底
3. `runDownload` 先按映射分组、逐组下载，结果按组汇总
4. 全局去重不变；老配置零迁移

## 人工测试清单（v1.2.0，上架/新版本发布前建议执行）

- [ ] 安装启用 → 设置填 API Key → 手动「立即下载」→ 附件库可预览
- [ ] 关键词映射：映射关键词进入独立策略/分组，未映射走全局
- [ ] 文章自动配图：发布带标签无封面文章 → 1-2 分钟后封面自动生成、图片入附件库
- [ ] 覆盖升级 1.1.0 → 配置保留
- [ ] 关闭定时/自动配图 → 不再触发
